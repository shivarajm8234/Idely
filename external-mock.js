/**
 * External Mock JavaScript Asset loaded dynamically by the web page.
 * Contains exposed API credentials.
 */

(function() {
  console.log("Mock script initialized.");

  // Exposed Stripe secret key in external file context
  const STRIPE_KEY_EXPOSED = "sk_test_thisIsAMockKeyForTestingPurposes";

  // Exposed AWS Access Key
  const AWS_ACCESS_KEY = "AKIAJHETRD6287FUDHSQ";
})();
